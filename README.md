# spring-data-pattern
A reusable Spring Boot library that centralizes database connectivity, datasource configuration, JPA setup, migrations, and common persistence configurations across applications.
